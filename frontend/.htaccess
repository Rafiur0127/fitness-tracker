RewriteEngine On

# Keep existing bookmarked PHP page URLs working while the browser UI is static.
RewriteRule ^(login|signup|dashboard|workout_logger|watertracker|goals|nutrition|Workout_plans|friend_challenges)\.php$ /$1.html [R=302,L]
RewriteRule ^$ /login.html [L]
